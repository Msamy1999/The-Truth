# Generate a long random value and store it as a secret in the hosting panel.
PAYLOAD_SECRET=replace-with-a-long-random-secret

# The production container persists this directory as a named volume.
DATABASE_URI=file:/app/data/payload.db

# Public canonical URL; this value is intentionally visible to browsers.
NEXT_PUBLIC_SITE_URL=https://the-straight-path.169.58.54.165.sslip.io
